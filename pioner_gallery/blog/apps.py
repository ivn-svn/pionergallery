from django.apps import AppConfig

from django.apps import AppConfig


class BlogConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'pioner_gallery.blog'  # Make sure this is set correctly
